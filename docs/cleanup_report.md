# Cleanup report — Anisotropia Direcional

**Date:** 2026-05-20  
**Scope:** Documentation alignment and prudent housekeeping. **No metric formula changes.** **No intentional numerical output changes.**

---

## 1. Baseline status (Phase 0)

| Check | Result |
|-------|--------|
| `pytest tests/ -q` | **141 passed** (2026-06-03) |
| Coverage (`--cov=anisotropia`) | **84.80%** (threshold 78%) |
| `compare_reference_outputs.py` | **Passed** (grand staff skip documented) |
| `reproduce_tables.py` | **Passed** (2 rows written) |
| `ruff check` | **Passed** (after removing 2 dead imports in `parsing.py`) |
| Git | **Not a repository** at project root |

---

## 2. Files archived

Moved to `archive/docs_legacy/` (not deleted):

| File | Reason |
|------|--------|
| `MANUAL_TECNICO.mhtml` | Obsolete StackEdit export; canonical `MANUAL_TECNICO.md` |
| `MANUAL_METRICAS.mhtml` | Same |
| `CORPUS_REFERENCIA.mhtml` | Same |

Created: `archive/README.md`, empty placeholders `scripts_legacy/`, `outputs_legacy/`, `notes_legacy/`.

---

## 3. Files removed

| Path | Type | Reason |
|------|------|--------|
| `.pytest_cache/` | cache | Regenerated by pytest |
| `.ruff_cache/` | cache | Regenerated by ruff |

**Not removed:** `corpus/reference_outputs/*`, frozen checksums, test fixtures, `corpus/tables/benchmark_summary.csv`.

---

## 4. Functions deprecated or documented (not removed)

| Item | Action |
|------|--------|
| `transitions_from_events` | Docstring: legacy; prefer `build_directional_transition_tables` / `run_analysis` |
| `legacy_mixed_mode` | Kept; documented |
| `include_attached` | Kept as explicit error via `validate_grace_policy` |
| `format_references_manual_*` | Kept as doc-sync helpers |

**No** `DeprecationWarning` at call time (would affect test noise); documentation-only deprecation for legacy transition builder.

---

## 5. Docs updated

| File | Changes |
|------|---------|
| `README.md` | 141 tests, ruff in CI, 0 official benchmarks, cleanup doc links |
| `MANUAL_TECNICO.md` | v2.4.0, `run_analysis` in §2 and new §12.3, pipeline file list |
| `MANUAL_METRICAS.md` | Points to `run_analysis` |
| `docs/anisotropia_current_rating.md` | 141 tests, ~85% coverage |
| `docs/STREAMLIT_PIPELINE_MIGRATION.md` | 88/100, 0 official benchmarks |
| `docs/cleanup_inventory.md` | Created (inventory + final actions) |
| `docs/cleanup_report.md` | This file |
| `archive/README.md` | Archive policy |

**Active docs not archived:** all listed in task spec remain at original paths.

---

## 6. Scripts updated

| Script | Change |
|--------|--------|
| `corpus/scripts/*` | No moves; already documented and CI-used |
| `run.bat` | Unchanged (Streamlit launcher) |

---

## 7. Code touched (non-formula)

| File | Change |
|------|--------|
| `anisotropia/parsing.py` | Legacy docstring; removed unused `warnings` / `GracePolicyNotImplementedError` imports |
| `anisotropia/references.py` | Clarified helper docstring |
| `tests/test_validation.py` | Clarified legacy-path purpose |
| `anisotropia/report.py` | **Not modified** this pass |

**`Anisotropia.py`:** No duplicate orchestration removed — already uses `run_analysis` only for metrics; visualization/report wiring retained.

---

## 8. `.gitignore` changes

Added: `*.mhtml`, `.ruff_cache/`, `.mypy_cache/`, `*.local.md`, `reports_local/`.

---

## 9. Formula / output status

| Question | Answer |
|----------|--------|
| Metric formulas changed? | **No** |
| Numerical outputs changed? | **No** (frozen compare passed; `reproduce_tables` re-ran CSV only) |
| Analytical defaults changed? | **No** |
| Scientific claim broadened? | **No** |
| Rating claim | **88/100** unchanged |

---

## 10. Final verification commands

```text
pytest tests/ -q                          → 141 passed
pytest tests/ --cov=anisotropia ...     → 84.80%, fail-under 78% OK
python corpus/scripts/compare_reference_outputs.py  → passed
python corpus/scripts/reproduce_tables.py           → passed
ruff check anisotropia tests corpus/benchmark_profile.py  → passed
python -c "from anisotropia import AnalysisConfig, run_analysis; print('ok')"  → ok
```

---

## 11. Remaining cleanup candidates (future, medium risk)

| Item | Risk | Notes |
|------|------|-------|
| Migrate `test_integration` / `test_validation` to `run_analysis` only | medium | Would change test path, not necessarily outputs |
| `st.cache_data` on `run_analysis` in Streamlit | low | Performance only |
| Licensed benchmark corpus + frozen outputs | N/A | Primary **90+** blocker |
| Auto-sync manuals from `references.format_references_manual_*` | low | Tooling |
| `parsing.py` coverage (70%) | low | More edge fixtures, not cleanup |

---

## 12. Link check

- Grep: **no** active Markdown links to `.mhtml` files.
- Archived paths not linked from active docs.

---

## 13. Identity preserved

Anisotropia Direcional remains a **systematic notational directional-field analyzer** for symbolic MusicXML pitch–time transitions — not audio, perception, harmony, orchestration, or general texture analysis.
